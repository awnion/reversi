{
  "name": "@reversi/bot-minimax",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build:wasm": "cd ../../rust/minimax && wasm-pack build --target web --out-dir ../../packages/bot-minimax/wasm",
    "build": "bun run build:wasm && bunx tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "@reversi/core": "workspace:*"
  }
}
