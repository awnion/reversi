{
  "name": "@reversi/bot-alphazero",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build:wasm": "wasm-pack build --target web ../../rust/alphazero --out-dir $(cd ../.. && pwd)/packages/bot-alphazero/wasm",
    "build": "bun run build:wasm && bunx tsc -b",
    "test": "bun test"
  },
  "dependencies": {
    "@reversi/core": "workspace:*"
  }
}
